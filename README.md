```
# 📣 بوت البث المتقدم (Broadcast Bot) 
**لإرسال رسائل منسقة (Embeds) إلى القنوات أو خاص للأعضاء مع دعم الأزرار والجدولة والقوالب**

---

## 📖 وصف عام
بوت Discord متكامل يتيح للأعضاء المصرح لهم (Role + Administrator) إنشاء رسائل إعلانية (Embeds) مع إضافة أزرار تفاعلية (CTA) وإرسالها إما إلى قناة نصية محددة أو عبر الرسائل الخاصة (DM) لجميع الأعضاء أو حسب حالتهم (Online/Offline).  
يدعم البوت حفظ الرسائل كقوالب (Templates)، وجدولة الإرسال لوقت لاحق، وإدارة الوظائف المجدولة (Jobs)، مع نظام كوول داون (Cooldown) وحد يومي للحد من الاستخدام.

---

## ✨ المميزات
- إنشاء Embed مخصص (عنوان، وصف، لون، صورة، تذييل).
- إضافة أزرار (CTA) تصل إلى 25 زراً (روابط خارجية).
- اختيار الهدف:
  - **Channel**: إرسال إلى قناة نصية معينة.
  - **DM**: إرسال خاص للأعضاء مع فلترة (All / Online / Offline).
- **الجدولة**: إرسال الرسائل في وقت محدد (صيغ: `in 10m`, `in 2h`, `in 1d` أو تاريخ محدد مثل `2026-02-08 21:30`).
- **القوالب (Templates)**: حفظ الرسائل المستخدمة بشكل متكرر واسترجاعها بسرعة.
- **إدارة الوظائف (Jobs)**: عرض الوظائف المجدولة وإلغاؤها.
- **نظام صلاحيات**:
  - يتطلب Role محدد في `config.json`.
  - بالإضافة إلى صلاحية Administrator.
- **حدود استخدام**:
  - Cooldown بين الإرسالات (قابل للتعديل).
  - حد يومي للإرسال (قابل للتعديل).
- **واجهة تفاعلية** عبر الأزرار والقوائم المنسدلة.
- **دعم API** (اختياري) لإرسال البث عبر طلبات HTTP.

---

## 🛠️ المتطلبات
- Node.js الإصدار 16.9.0 أو أحدث.
- حساب Discord بوت (مع التوكن).
- تفعيل البوابات (Intents) في بوابة المطور:
  - `Guilds`
  - `GuildMessages`
  - `MessageContent`
  - `GuildMembers`
  - `GuildPresences` (اختياري لتصفية الحالة).

---

## ⚙️ الإعداد والتشغيل

### 1. تنزيل الملفات
ضع جميع الملفات في مجلد واحد:
- `index.js`
- `config.json`
- `package.json`
- `package-lock.json` (اختياري)
- `start.bat` (اختياري لتشغيل سريع على Windows)

### 2. تثبيت الحزم
افتح terminal في المجلد ونفذ:
```bash
npm install
```

### 3. إعداد `config.json`
```json
{
    "allowedRoleId": "معرف_الرول_المصرح",
    "TOKEN": "توكن_البوت",
    "image": "رابط_صورة_للوحة_التحكم",
    "cooldownSeconds": 30,
    "dailyLimit": 30,
    "api": {
        "enabled": false,
        "port": 3000,
        "token": "مفتاح_API_سري"
    }
}
```
- **allowedRoleId**: اتركه فارغاً إذا أردت السماح للجميع (لا ننصح).
- **TOKEN**: توكن البوت من [Discord Developer Portal](https://discord.com/developers/applications).
- **image**: رابط صورة تظهر في اللوحة الرئيسية (اختياري).
- **cooldownSeconds**: عدد الثواني التي يجب أن ينتظرها العضو بين كل إرسال وآخر.
- **dailyLimit**: الحد الأقصى لعدد الإرسالات لكل عضو في اليوم.
- **api**: إعدادات الـ REST API (يمكن تعطيلها).

### 4. تشغيل البوت
```bash
node index.js
```
أو `start.bat` على Windows.

إذا رأيت `✅ bot is ready: ...` فهذا يعني أن البوت يعمل بنجاح.

---

## 📱 استخدام البوت

### فتح لوحة التحكم
في أي سيرفر يتواجد فيه البوت، اكتب الأمر التالي في أي قناة نصية:
```
!b
```
ستظهر لك لوحة تحتوي على أزرار:
- **➕ Broadcast جديد** – لإنشاء رسالة جديدة.
- **📦 Templates** – لحفظ أو تحميل أو حذف قالب.
- **⏰ Jobs** – لعرض وإلغاء الوظائف المجدولة.
- **ℹ️ Help** – لعرض مساعدة سريعة.

### إنشاء رسالة جديدة
1. اضغط على **➕ Broadcast جديد**.
2. ستظهر نافذة (Modal) لملء بيانات الـ Embed:
   - Title (اختياري)
   - Description (إجباري)
   - Color (hex مثل #716360)
   - Image URL (اختياري)
   - Footer (اختياري)
3. بعد الإرسال، يظهر **معاينة** للرسالة مع أزرار التحكم:
   - **🎯 Target** – اختيار بين Channel أو DM.
   - **📌 اختيار قناة** (إذا كان الهدف Channel).
   - **👤 DM Mode** (إذا كان الهدف DM) – All / Online / Offline.
   - **➕ Add CTA** – إضافة زر (يمكن تكرار الضغط لإضافة عدة أزرار).
   - **🧹 Clear CTA** – مسح جميع الأزرار.
   - **⏰ جدولة** – لتعيين وقت لاحق للإرسال.
   - **✅ إرسال الآن** – لعرض تأكيد الإرسال الفوري.
4. بعد الضغط على **✅ إرسال الآن**، يظهر تأكيد بهدف الإرسال، اضغط **✅ Confirm إرسال** ليتم الإرسال.
5. بعد الإرسال (أو الجدولة) سيتم حذف المسودة (Draft).

### القوالب
- **💾 حفظ Template من Draft** – يحفظ المسودة الحالية كقالب باسم تحدده.
- **📥 تحميل Template** – يعرض قائمة القوالب المحفوظة لتحميلها كمسودة جديدة.
- **🗑️ حذف Template** – يعرض قائمة لحذف قالب.

### الوظائف (Jobs)
- **📄 عرض Jobs** – يعرض آخر 10 وظائف (مجدولة، مرسلة، ملغاة).
- **🛑 إلغاء Job** – يسمح بإلغاء وظيفة مجدولة قبل تنفيذها.

---

## ⏱️ نظام الحدود (Cooldown & Daily Limit)
- **Cooldown**: بعد كل إرسال ناجح (فوري أو مجدول)، يجب على العضو الانتظار المدة المحددة قبل إرسال آخر.
- **الحد اليومي**: كل عضو لديه حد أقصى لعدد الإرسالات في اليوم (حسب المنطقة الزمنية للبوت). يتم تحديث العداد يومياً.

> يتم تطبيق الحدود فقط عند **الإرسال الفعلي** (أو الجدولة) وليس على المعاينة أو التعديل.

---

## 🔌 API (اختياري)
إذا فعّلت `api.enabled` في `config.json`، يستمع البوت على المنفذ المحدد لاستقبال طلبات POST على المسار `/broadcast`.  
يجب إضافة header:
```
x-api-key: القيمة_الموجودة_في_config.api.token
```
مثال body (JSON):
```json
{
  "type": "channel",
  "guildId": "123456789012345678",
  "channelId": "876543210987654321",
  "embedData": {
    "title": "عنوان",
    "description": "وصف",
    "color": "#ff0000",
    "imageUrl": "https://...",
    "footer": "تذييل"
  },
  "ctas": [
    { "label": "رابط 1", "url": "https://example.com" }
  ]
}
```
لإرسال DM:
```json
{
  "type": "dm",
  "guildId": "...",
  "dmMode": "online",
  "embedData": { ... }
}
```

---

## 🧪 هيكل الملفات
- `config.json` – الإعدادات الأساسية.
- `templates.json` – يُنشئ تلقائياً لحفظ القوالب.
- `jobs.json` – يُنشئ تلقائياً لحفظ الوظائف المجدولة.

---

## ⚠️ ملاحظات مهمة
- **الصلاحيات**: يحتاج البوت إلى صلاحية `Administrator` في السيرفر (أو صلاحيات كافية لإرسال الرسائل ورؤية الأعضاء).
- **Presence Intent**: لتصفية الأعضاء حسب الحالة (Online/Offline)، يجب تفعيل `GuildPresences` في بوابة المطور وفك تعليق السطر في `index.js` (السطر 54).
- **الحدود**: يتم تخزين العدادات في الذاكرة فقط (Map). إذا أعادت تشغيل البوت، يتم إعادة تعيين العدادات اليومية.
- **الأزرار**: الحد الأقصى 25 زراً (5 صفوف × 5 أزرار).

---


## 📧 الدعم والتواصل
للاستفسارات أو الدعم الفني، انضم إلى سيرفر الديسكورد الخاص بنا:
[https://discord.gg/HCskVYZPtB](https://discord.gg/HCskVYZPtB)

البوت من تطوير: **ii_abual3bed | STDEV**

